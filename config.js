const config = {
  // command prefix
  // exp. $on
  "prefix": "$",
  // show, haruka, hikari, takeru, santa, bear
  "speaker": "takeru",
  // 50 <= x <= 200
  "pitch": 100,
  // 50 <= x <= 400
  "speed": 100,
  // TTS (Text To Speech) enabled ?
  "yomu": true,
  // STT (Speech To Text) enabled ?
  "kaku": true,
}
module.exports = config;